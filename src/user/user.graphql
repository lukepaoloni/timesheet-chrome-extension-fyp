scalar DateTime

type User {
    id: ID!
    email: String!
    name: String!
    role: String!
    status: String!
    createdAt: DateTime!
    updatedAt: DateTime!
    lastLoggedIn: DateTime!
}

type Query {
    getUsers: [User!]!
}